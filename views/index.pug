extends layout

block content
  h1= title
  p My name is Vedha Sri Gaddam
  body 
  button(type="button", class="btn btn-success" onclick ="doBump()") Press Me

  div#bump Bump the count 
  a(href="Earring") Earring Results
  script.  
    var count = 0
    var bump = 1
    function doBump() {
      count = count + bump 
      bump = bump + 1
      document.getElementById("bump").innerHTML = "Count "+ count +" Bump "+bump;
    }
  
 